<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Un color canela muy translúcido para que se integre con el fondo -->
    <solid android:color="#4DD2B48C"/>

    <!-- Bordes muy redondeados para que parezca una píldora -->
    <corners android:radius="20dp"/>

    <!-- Pequeño espaciado interno para que el texto no toque los bordes -->
    <padding
        android:left="12dp"
        android:right="12dp"
        android:top="4dp"
        android:bottom="4dp"/>

</shape>
