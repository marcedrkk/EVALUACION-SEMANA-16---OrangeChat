<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient_voidchat"
    android:padding="24dp"
    tools:context=".MainActivity">

    <!-- 1. El Logo (ImageView) -->
    <ImageView
        android:id="@+id/login_logo"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_above="@id/login_title"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp"
        app:srcCompat="@mipmap/ic_launcher_round" />

    <!-- 2. El Título de la App (TextView) -->
    <TextView
        android:id="@+id/login_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:fontFamily="sans-serif-light"
        android:text="VoidChat"
        android:textColor="#5D4037"
        android:textSize="48sp"
        android:textStyle="bold" />

    <!-- 3. El Botón de Ingreso -->
    <Button
        android:id="@+id/btnGoogle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/login_title"
        android:layout_marginTop="32dp"
        android:background="@drawable/bubble_send"
        android:padding="14dp"
        android:text="INGRESAR"
        android:textColor="@android:color/white"
        android:textSize="16sp"
        app:backgroundTint="#FFCB7F" />

</RelativeLayout>

